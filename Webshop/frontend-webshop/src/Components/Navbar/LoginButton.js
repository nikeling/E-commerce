import {Link} from "react-router-dom"

function LoginButton() {

    return (
        <div>
        <button>
            <a href = "/login">
            Login
            </a>

        </button>

       </div>
    )
}

export default LoginButton;